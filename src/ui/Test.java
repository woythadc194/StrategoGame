package ui;

public class Test {
	
	public static void main( String[] args ){
		/*
		ArrayList<Piece> redList = new ArrayList<Piece>();
		ArrayList<Piece> bluList = new ArrayList<Piece>();
		for( int i=1; i<10; i++ )
			redList.add( new Piece( (char)i, 0, 0, "RED" ) );
		for( int i=1; i<10; i++ )
			bluList.add( new Piece( (char)i, 0, 0, "BLUE" ) );
		redList.add( new Piece( 'S', 0, 0, "RED" ) );	
		redList.add( new Piece( 'B', 0, 0, "RED" ) );	
		redList.add( new Piece( 'F', 0, 0, "RED" ) );

		bluList.add( new Piece( 'S', 0, 0, "BLUE" ) );	
		bluList.add( new Piece( 'B', 0, 0, "BLUE" ) );	
		bluList.add( new Piece( 'F', 0, 0, "BLUE" ) );
		
		for( Piece rp : redList )
			for( Piece bp : bluList ){
				String rv = "" + rp.getVal();
				if( Integer.parseInt( rv ) > 9 )
					rv = "" + (char)Integer.parseInt( rv );

				String bv = "" + bp.getVal();
				if( Integer.parseInt( bv ) > 9 )
					bv = "" + (char)Integer.parseInt( bv );
				
				System.out.println( "Red " + rv + " vs Blue " + bv + ": " + rp.battle( bp ) );
			}
		System.out.println( redList.get(0).getVal() );
		System.out.println( redList.get(0).getX() );

		System.out.println( redList.get(0).getY() );

		System.out.println( redList.get(0).isShowing() );
		*/
	}
}